{
  "name": "bus_arrival-dictionary",
  "version": "1.0.0",
  "license": "ISC",
  "author": "yc",
  "scripts": {
    "serve:client": "npm run serve --prefix client",
    "serve:server": "npm run serve --prefix server",
    "serve": "npm-run-all --parallel serve:server serve:client",
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client",
    "build": "npm-run-all build:server build:client",
    "start": "node ./server/dist/server.js"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
